import { HomeView } from '@/views/home-views/home-view'

export default function Home() {
  // All data fetching happens here at the page level

  // Get data that ItemsSection needs (full arrays)

  // Get counts for EquipmentSection

  // Buildings section needs

  return <HomeView />
}
