// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { TableCache, deepEqual } from '@clockworklabs/spacetimedb-sdk'
import { type EventContext } from '.'
import { BuildingRepairsDesc } from './building_repairs_desc_type'

/**
 * Table handle for the table `building_repairs_desc`.
 *
 * Obtain a handle from the [`buildingRepairsDesc`] property on [`RemoteTables`],
 * like `ctx.db.buildingRepairsDesc`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.buildingRepairsDesc.on_insert(...)`.
 */
export class BuildingRepairsDescTableHandle {
  tableCache: TableCache<BuildingRepairsDesc>

  constructor(tableCache: TableCache<BuildingRepairsDesc>) {
    this.tableCache = tableCache
  }

  count(): number {
    return this.tableCache.count()
  }

  iter(): Iterable<BuildingRepairsDesc> {
    return this.tableCache.iter()
  }
  /**
   * Access to the `cargoId` unique index on the table `building_repairs_desc`,
   * which allows point queries on the field of the same name
   * via the [`BuildingRepairsDescCargoIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.buildingRepairsDesc.cargoId().find(...)`.
   *
   * Get a handle on the `cargoId` unique index on the table `building_repairs_desc`.
   */
  cargoId = {
    // Find the subscribed row whose `cargoId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): BuildingRepairsDesc | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.cargoId, col_val)) {
          return row
        }
      }
    }
  }

  onInsert = (cb: (ctx: EventContext, row: BuildingRepairsDesc) => void) => {
    return this.tableCache.onInsert(cb)
  }

  removeOnInsert = (cb: (ctx: EventContext, row: BuildingRepairsDesc) => void) => {
    return this.tableCache.removeOnInsert(cb)
  }

  onDelete = (cb: (ctx: EventContext, row: BuildingRepairsDesc) => void) => {
    return this.tableCache.onDelete(cb)
  }

  removeOnDelete = (cb: (ctx: EventContext, row: BuildingRepairsDesc) => void) => {
    return this.tableCache.removeOnDelete(cb)
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: BuildingRepairsDesc, newRow: BuildingRepairsDesc) => void) => {
    return this.tableCache.onUpdate(cb)
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: BuildingRepairsDesc, newRow: BuildingRepairsDesc) => void) => {
    return this.tableCache.removeOnUpdate(cb)
  }
}
