// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { TableCache, deepEqual } from '@clockworklabs/spacetimedb-sdk'
import { type EventContext } from '.'
import { EmpireEmblemState } from './empire_emblem_state_type'

/**
 * Table handle for the table `empire_emblem_state`.
 *
 * Obtain a handle from the [`empireEmblemState`] property on [`RemoteTables`],
 * like `ctx.db.empireEmblemState`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.empireEmblemState.on_insert(...)`.
 */
export class EmpireEmblemStateTableHandle {
  tableCache: TableCache<EmpireEmblemState>

  constructor(tableCache: TableCache<EmpireEmblemState>) {
    this.tableCache = tableCache
  }

  count(): number {
    return this.tableCache.count()
  }

  iter(): Iterable<EmpireEmblemState> {
    return this.tableCache.iter()
  }
  /**
   * Access to the `entityId` unique index on the table `empire_emblem_state`,
   * which allows point queries on the field of the same name
   * via the [`EmpireEmblemStateEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.empireEmblemState.entityId().find(...)`.
   *
   * Get a handle on the `entityId` unique index on the table `empire_emblem_state`.
   */
  entityId = {
    // Find the subscribed row whose `entityId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): EmpireEmblemState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.entityId, col_val)) {
          return row
        }
      }
    }
  }

  onInsert = (cb: (ctx: EventContext, row: EmpireEmblemState) => void) => {
    return this.tableCache.onInsert(cb)
  }

  removeOnInsert = (cb: (ctx: EventContext, row: EmpireEmblemState) => void) => {
    return this.tableCache.removeOnInsert(cb)
  }

  onDelete = (cb: (ctx: EventContext, row: EmpireEmblemState) => void) => {
    return this.tableCache.onDelete(cb)
  }

  removeOnDelete = (cb: (ctx: EventContext, row: EmpireEmblemState) => void) => {
    return this.tableCache.removeOnDelete(cb)
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: EmpireEmblemState, newRow: EmpireEmblemState) => void) => {
    return this.tableCache.onUpdate(cb)
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: EmpireEmblemState, newRow: EmpireEmblemState) => void) => {
    return this.tableCache.removeOnUpdate(cb)
  }
}
